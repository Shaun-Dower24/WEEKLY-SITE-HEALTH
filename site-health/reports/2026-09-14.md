# Site health — week of 2026-09-14

## At a glance
- arch — baseline established, no findings, uptime 100% (full 7-day coverage)
- paw — baseline established, 1 P1 (routine config, not client-visible), uptime UNAVAILABLE
- dower — baseline established, no findings, uptime UNAVAILABLE

## Data sources
- **Firecrawl** — available and used for all three sites this run (last week's run
  aborted here; the connector is now enabled for this session).
- **Sentry** — connected. `arch-catering` project exists and was queried (0
  unresolved issues, 0 new). `paw` and `dower` are configured as Sentry: NONE
  and were skipped entirely per the routine's rules — not reported as "0
  errors" since no project is bound to them.
- **UptimeRobot** — connected. Only `archcateringandrefrigeration.co.uk` has a
  monitor (id 803742553, up 30+ days, 100% over the last 7 days, 0 incidents —
  full coverage). No monitor exists for `positive-animal-wellbeing.netlify.app`
  or `dowerdigital.co.uk`; both are UPTIME_UNAVAILABLE this run — a plumbing
  gap, not a downtime claim.
- **Google Drive** — available. Wrote the arch drive_file. Note: the file
  already contained data from a prior write dated 2026-09-07
  (`runs_this_month: 1`, no findings) with no corresponding baseline or commit
  anywhere in this repo's git history (checked both branches). This run
  proceeded as a FIRST RUN per the repo's own state (no baseline.json existed)
  and incremented the Drive run counter to 2 to keep the monthly count
  accurate, rather than resetting it. Cause of the untracked prior write is
  unknown — flagging for awareness, not asserting a mechanism.
- GSC/search performance — out of scope for this routine by design.

## Config
- **paw — configured url does not match the site's declared canonical
  domain (P1, routine config bug).** The routine has paw's `url` set to
  `https://positive-animal-wellbeing.netlify.app`. That page's own
  `<link rel="canonical">` tag, its `robots.txt` `Sitemap:` directive, every
  URL in its `sitemap.xml`, and its `llms.txt` all declare
  `https://pawellbeing.com` as the real domain. The routine audited the
  configured (Netlify) URL as instructed, so this run's paw data reflects the
  alias, not necessarily the production host in search engines' eyes.
  **Corrected value to paste in:** `url: https://pawellbeing.com`
  This is not a defect on the site — client_visible=false in open-findings.

## arch
First run for this site — no prior baseline existed, so this establishes one
rather than reporting a diff.

**Current state:**
- All 3 configured pages (`/`, `/commercial-dishwasher`, `/contacts`) scraped
  successfully — no 404s or soft-404s. Each has a title, meta description,
  exactly one H1, a canonical tag matching the configured domain, and
  LocalBusiness/Service/BreadcrumbList JSON-LD.
- robots.txt explicitly allows Googlebot (via the wildcard rule), GPTBot,
  ClaudeBot, PerplexityBot, GoogleOther and CCBot; only PDF policy documents
  are disallowed. Sitemap directive and sitemap.xml both declare the
  configured domain — no config mismatch.
- llms.txt present and accurate.
- **Accreditation check: no violation.** The pages reference "F-Gas
  servicing", "Gas Safety inspections" and "CP42 certification" as services
  performed for customers' equipment, and llms.txt explicitly attributes Gas
  Safe/F-Gas/electrical qualifications to subcontracted engineers, not the
  company itself. This matches the "service description, not an
  accreditation claim" exception in the rule — no removal needed.
- Sentry (`arch-catering` project): 0 unresolved issues, 0 new.
- UptimeRobot: 100% uptime over the last 7 days (2026-09-07 to 2026-09-14),
  monitor has been up for 30+ days, 0 incidents. Full coverage.

### Still outstanding
None.

## paw
First run for this site — no prior baseline existed.

**Current state:**
- Homepage scraped successfully. Title, meta description, canonical, one H1,
  and LocalBusiness/Person/Service/FAQPage JSON-LD all present.
- robots.txt explicitly welcomes Googlebot, Bingbot, GPTBot, ClaudeBot,
  PerplexityBot, GoogleOther, CCBot and others. Sitemap and llms.txt present.
- No accreditation rule to police for this site.
- Sentry: NONE — skipped, not reported as clean.
- UptimeRobot: UNAVAILABLE — no monitor exists for this site.

### P1
- Configured url vs. declared canonical domain mismatch — see **Config**
  section above. (Routine config issue, not a site defect.)

### Still outstanding
- 2026-09-14-paw-wrong-configured-host (P1, routine config) — 0 days old,
  found this run.

## dower
First run for this site — no prior baseline existed. Pages list was AUTO;
resolved from sitemap.xml as homepage + the three highest-priority
service/commercial pages (`/web-design/`, `/website-care/`, `/seo/`, all at
priority 0.9), capped at 4. `/web-design-stevenage/` (0.8), `/faq/` and
`/privacy/` were left out by the cap and priority ranking. This list is now
recorded in baseline.json and will stay stable between runs.

**Current state:**
- All 4 resolved pages scraped successfully. Each has a title, meta
  description, canonical tag matching the configured domain, exactly one H1,
  and Service/BreadcrumbList (or ProfessionalService/FAQPage on the
  homepage) JSON-LD.
- robots.txt explicitly allows Googlebot (wildcard), GPTBot, ClaudeBot,
  PerplexityBot, GoogleOther and CCBot. Sitemap directive, sitemap.xml and
  llms.txt all declare `dowerdigital.co.uk` — matches the configured url, no
  config mismatch.
- No accreditation rule to police for this site.
- Sentry: NONE — skipped, not reported as clean.
- UptimeRobot: UNAVAILABLE — no monitor exists for this site.

### Still outstanding
None.

## Nothing changed
N/A — first run for all three sites this week; see per-site sections above
for the baseline established.
