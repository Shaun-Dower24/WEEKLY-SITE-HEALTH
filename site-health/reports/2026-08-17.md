# Site health — week of 2026-08-17

## At a glance
- arch — baseline established, no findings, uptime 100% (partial: ~47h coverage)
- paw — baseline established, 1 P1 (routine config, not client-visible), uptime UNAVAILABLE
- dower — baseline established, no findings, uptime UNAVAILABLE

## Data sources
- **Firecrawl** — available and used this run (unavailable last run, 2026-08-15).
- **Sentry** — connected. Org `dower-digital` now has two projects: `arch-catering` and `paw`. Per this routine's config, arch is bound to `arch-catering`; paw is still configured `sentry: NONE` here, so paw's Sentry data was not queried this run (a `paw` project now existing in Sentry is not itself acted on — the routine's own config governs, not Sentry's project list).
- **UptimeRobot** — connected. One monitor exists, for `archcateringandrefrigeration.co.uk` (created 2026-08-15, so this run's window is partial — see below). No monitors exist for `positive-animal-wellbeing.netlify.app` or `dowerdigital.co.uk` — UPTIME_UNAVAILABLE for both, noted once here as a plumbing gap, not inferred from Firecrawl reachability.
- **Google Drive** — available. `arch`'s hand-off file was updated in place (stale 2026-08-15 revision trashed).
- GSC/Windsor was not queried, per this routine's scope (search performance belongs to the monthly report).

## Config
- **paw — wrong host audited (P1 CONFIG).** This routine is configured against `https://positive-animal-wellbeing.netlify.app`, but the site's own canonical tag, robots.txt `Sitemap:` directive, `sitemap.xml` URLs and `llms.txt` all declare `https://pawellbeing.com` as the real domain. The netlify.app homepage still resolves directly (200, no redirect) but `robots.txt`/`sitemap.xml`/`llms.txt` requests on it 301 through to pawellbeing.com. This run continued auditing the configured netlify.app URL as instructed, so the diff stays comparable, but going forward the config should point at the live domain.
  **Corrected value to paste in:** `url: https://pawellbeing.com`
  This is a routine configuration issue, not a defect on the site — logged with `client_visible: false`.

## arch
First run — baseline established for `archcateringandrefrigeration.co.uk`. Pages checked (explicit list): `/`, `/commercial-dishwasher`, `/contacts`.

- All 3 pages scraped cleanly (200, real content, 1 H1 each, title + meta description present, JSON-LD `LocalBusiness`/`Service`/`FAQPage`/`BreadcrumbList` present).
- Configured URL matches the domain declared in canonical tags, robots.txt `Sitemap:`, and sitemap.xml — no CONFIG issue.
- robots.txt welcomes Googlebot and all named AI crawlers (GPTBot, ClaudeBot, PerplexityBot, GoogleOther, CCBot); only `/PDF's/` and `*.pdf` are disallowed. llms.txt and sitemap.xml both present.
- **Accreditation check (P1 rule): no violation.** Copy across the 3 pages references "Gas Safety" as a service name and "F-Gas servicing" as a service offered — both service descriptions, not accreditation claims — and matches llms.txt's own statement that gas/F-Gas/electrical work is carried out by qualified, appropriately registered *subcontract* engineers. No page claims the company itself holds Gas Safe, F-Gas or NICEIC registration.
- Sentry (`arch-catering`): one issue, "This is your first error!", already resolved, culprit `/`, 1 event — this reads as Sentry's own test event from project setup, not a real production error, and it's not on a conversion path. No action needed; not logged as a finding.
- Uptime: monitor created 2026-08-15 (this run's coverage window is **partial: ~47h**, 2026-08-15 01:06 UTC–2026-08-17 00:00 UTC). **100% uptime, 0 incidents** over that window — reported honestly as partial, not as a clean full week.

### Still outstanding
None.

## paw
First run — baseline established for `positive-animal-wellbeing.netlify.app`. Page checked: `/`.

- Homepage scraped cleanly (200, real content, 1 H1, title + meta description present, rich JSON-LD including `LocalBusiness`, `Person`, `Service`, `FAQPage`, `Review`).
- robots.txt/llms.txt/sitemap.xml all present and welcome Googlebot and all named AI crawlers, no disallows.
- Accreditation: none to police per config — not checked.
- Sentry: not queried (configured NONE for this site).
- Uptime: UPTIME_UNAVAILABLE — no UptimeRobot monitor exists for this site.

### Still outstanding
- **[2026-08-17-paw-wrong-host-configured] P1 CONFIG**, 0 days old — routine points at the netlify.app alias instead of the site's declared canonical domain `pawellbeing.com`. See Config section above. Not client-visible (routine bug, not a site defect).

## dower
First run — baseline established for `dowerdigital.co.uk`. Pages list was `AUTO`; resolved from sitemap.xml as the homepage plus the 3 highest-priority service pages (capped at 4): `/`, `/web-design`, `/website-care`, `/seo`. This resolved list is now recorded in `baseline.json` and will stay stable between runs.

- All 4 pages scraped cleanly (200, real content, 1 H1 each, title + meta description present). Homepage carries a `ProfessionalService` JSON-LD graph (with `Person`, `OfferCatalog`, `FAQPage`); each service page carries its own `Service` + `BreadcrumbList` schema.
- Configured URL matches the domain declared in canonical tags, robots.txt `Sitemap:`, and sitemap.xml — no CONFIG issue.
- robots.txt welcomes Googlebot and all named AI crawlers, no disallows. llms.txt and sitemap.xml both present.
- Accreditation: none to police per config — not checked.
- Sentry: not queried (configured NONE for this site).
- Uptime: UPTIME_UNAVAILABLE — no UptimeRobot monitor exists for this site.

### Still outstanding
None.

## Nothing changed
N/A — first run for all three sites, no prior baseline existed. Future runs will diff against the baselines captured today.
