# Site health — week of 2026-08-15

## At a glance
- arch — BLOCKED, run aborted (plumbing)
- paw — BLOCKED, run aborted (plumbing)
- dower — BLOCKED, run aborted (plumbing)

## Data sources
- **Firecrawl — UNAVAILABLE.** The connector is authorized and connected at
  the org level (`installState: connected`) but `enabledInChat: false` for
  this routine's session. Per the routine's own run rules, page content,
  robots.txt/llms.txt/sitemap checks, and reachability all depend on
  Firecrawl, so this run aborts here rather than proceeding without it or
  fabricating figures. **Action needed:** enable the Firecrawl connector
  for this routine's chat session (this is a per-session toggle, separate
  from the org-level authorization, which is already done).
- **Windsor (GSC)** — connected and enabled in this session. Not queried
  this run because the abort happened before the per-site data-gathering
  step.
- **Sentry** — connected and enabled, but the organization
  (`dower-digital`) has exactly one project, `javascript-react`, which
  isn't bound to any specific site. None of the three sites in this
  routine currently have a real per-site Sentry project, so the error
  layer is effectively NONE for all of them. Per the routine's rules,
  this should stay off until real per-site projects exist, rather than
  report a misleading "0 errors."
- **UptimeRobot** — connected and enabled. One monitor exists, for
  `archcateringandrefrigeration.co.uk` (currently UP). No monitors exist
  yet for the `positive-animal-wellbeing.netlify.app` or
  `dower-digital.netlify.app` sites.

## arch / paw / dower
No findings — no page, crawl-directive, or search data was gathered this
run because Firecrawl was unavailable. No baseline was written; this
remains a FIRST RUN for all three sites until a run completes with
Firecrawl enabled.

## Nothing changed
N/A — first run for all sites, no baseline exists yet.
