# Site health — week of 2026-08-15

*Note: an earlier run today aborted because Firecrawl was not enabled in
that session (see prior commit). Firecrawl was available for this run, so
this report supersedes that aborted one and is the first completed run for
all three sites.*

## At a glance
- arch — OK, baseline established (no findings)
- paw — 1 finding, highest severity P1 (baseline established)
- dower — 1 finding, highest severity P1 (baseline established)

## Data sources
- **Windsor (GSC), arch** — UNAVAILABLE. The `searchconsole` connector
  returned a free-plan account-limit error ("Uh-oh! You've connected more
  accounts than your Free plan allows. Upgrade here...") instead of clicks/
  impressions/position data, on every query tried (with and without an
  explicit account filter). This is a connector-plan issue, not a traffic
  collapse — no P1 raised. No GSC clicks/impressions/position data was
  collected this run; nothing to diff next week either until this is
  resolved.
- **paw, dower** — gsc: NONE (not configured), not queried.
- **Sentry** — NONE for all three sites (no project configured); skipped
  entirely, not reported as "0 errors".

## Config
No CONFIG findings. arch and dower each declare the same domain in their
canonical tags, robots.txt `Sitemap:` directive, and sitemap.xml as their
configured url. paw has no robots.txt, sitemap.xml or llms.txt to check
against, so domain agreement can't be verified either way (see paw section).

## arch
*First run — baseline established.*

Homepage, `/commercial-dishwasher` and `/contacts` all returned 200 with a
unique `<title>`, meta description, exactly one `<h1>`, a matching
canonical, and LocalBusiness/Service/FAQPage/BreadcrumbList/HowTo JSON-LD.
robots.txt allows Googlebot and explicitly welcomes GPTBot, ClaudeBot,
PerplexityBot, GoogleOther, CCBot and anthropic-ai; only PDF paths are
disallowed. llms.txt and sitemap.xml (35 URLs) are present and correctly
scoped to archcateringandrefrigeration.co.uk.

Checked the three audited pages for false Gas Safe / F-Gas / NICEIC
accreditation claims (per the known constraint that Arch holds no direct
accreditations and uses subcontracted registered engineers): no such claims
found in visible copy or JSON-LD. The Gas Safe/F-Gas mentions present are
service-category references ("Gas Safety" service page, "F-Gas servicing"
offered), not claims of direct registration. No action needed.

No findings this run.

## dower
*First run — baseline established.*

Homepage returned 200 with title, meta description, one `<h1>`, matching
canonical, and ProfessionalService/WebSite/FAQPage JSON-LD. robots.txt,
llms.txt and sitemap.xml (6 URLs) are present and correctly scoped to
dowerdigital.co.uk, with Googlebot and AI crawlers (GPTBot, ClaudeBot,
PerplexityBot, Google-Extended, GoogleOther, Applebot-Extended, CCBot,
etc.) explicitly allowed.

### P1
- `/contact` returns HTTP 404 ("Page not found") — before → after: N/A
  (first run) — current state: the configured page does not resolve.
  Hypothesis: the site appears to be a single page with anchor sections
  (llms.txt lists contact as `https://dowerdigital.co.uk/#contact`), so a
  standalone `/contact` route may never have existed. Recommended action:
  either add a redirect/route for `/contact` to `/#contact`, or update the
  monitored page list to the real anchor URL — worth confirming which is
  intended.

## paw
*First run — baseline established.*

Homepage returned 200 with title, meta description and one `<h1>`, but has
no canonical tag and no JSON-LD structured data.

### P1
- `/contact` returns HTTP 404 ("Page not found") — before → after: N/A
  (first run) — current state: the configured page does not resolve.
  Recommended action: confirm the correct contact page path, or add the
  missing route/redirect.

### P3
- No robots.txt found (404). Recommended action: add one — currently there
  is no explicit crawl policy or `Sitemap:` directive for search/AI
  crawlers to discover. (Reported once; will be suppressed in future runs
  unless it changes.)
- No llms.txt found (404). (Reported once; will be suppressed in future
  runs unless it changes.)
- No sitemap.xml found (404). (Reported once; will be suppressed in future
  runs unless it changes.)

## Nothing changed
- arch — no findings.
