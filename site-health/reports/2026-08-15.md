# Site health — week of 2026-08-15

FIRST RUN for all three sites — baselines established this run. No diff is
possible yet; the sections below summarise current state. Next week's run
will diff against these baselines.

(This report supersedes an earlier 2026-08-15 report that aborted because
Firecrawl was not enabled in that session. Firecrawl was available this
run and all three sites were scraped live.)

## At a glance
- **arch** — baseline established, 0 site findings, uptime 100% (partial coverage, see below)
- **paw** — baseline established, 0 site findings, 1 P1 CONFIG, 3 P3, uptime UNAVAILABLE
- **dower** — baseline established, 0 site findings, 1 P1 CONFIG, uptime UNAVAILABLE

## Data sources
Plumbing gaps, not site health.

- **UptimeRobot — PARTIAL.** Exactly one monitor exists across all three
  sites: `archcateringandrefrigeration.co.uk` (id 803742553), created
  2026-08-15T01:06:21Z — roughly 7 hours before this run. Its 100% figure
  therefore covers ~7h, not a week. The account-level 7-day window reports
  100% with 0 incidents, but the monitor did not exist for most of that
  window, so it is not evidence of a clean week.
- **UptimeRobot — UNAVAILABLE for paw.** No monitor exists for
  `positive-animal-wellbeing.netlify.app`. Availability is unknown for this
  site; it is not inferred from Firecrawl succeeding.
- **UptimeRobot — UNAVAILABLE for dower.** No monitor exists for
  `dowerdigital.co.uk`. Same caveat.
- **Sentry — not queried.** All three sites are configured `sentry: NONE`,
  so the error layer was skipped entirely rather than reported as clean.
  The `dower-digital` org does have one project (`javascript-react`) but it
  is not bound to any of these sites, so it would not be meaningful here.
- **Google Drive — available.** The arch handoff file was written
  successfully (see Drive handoff below).
- **Firecrawl — available.** All 9 page fetches plus robots.txt, llms.txt
  and sitemap.xml checks completed for all three sites.

## Config
Findings where the routine is pointed at the wrong target. These are bugs
in this routine's configuration, not defects on the client sites, and are
marked `client_visible: false`.

### P1
- **paw — configured page `/contact` does not exist.** Configured page list
  includes `/contact`; the URL returns a hard HTTP 404 (Netlify's default
  not-found page). The homepage carries an in-page `#contact` section and
  form instead. *Hypothesis:* the page list was authored assuming a
  standalone contact page that this site never had. **Recommended action:**
  correct the routine's page list for paw — drop `/contact` or replace it
  with `/#contact`. Do not change the site.
- **dower — configured page `/contact` does not exist.** Same shape: hard
  HTTP 404 on `/contact`. The site's own declarations corroborate that no
  such page exists — `sitemap.xml` lists six URLs and none is `/contact`,
  and `llms.txt` references contact as the on-page anchor `/#contact`.
  *Hypothesis:* same authoring assumption as paw. **Recommended action:**
  correct the routine's page list for dower — drop `/contact` or replace it
  with `/#contact`. Do not change the site.

No domain/canonical mismatches were found on any site: for arch and dower,
canonical tags, the robots.txt `Sitemap:` directive, sitemap.xml URLs and
llms.txt all agree with the configured host. paw declares no domain
anywhere (no canonical, no robots.txt, no sitemap, no llms.txt), so there
is nothing to contradict its configured URL.

## arch
No findings. Baseline established.

Current state: all three configured pages (`/`, `/commercial-dishwasher`,
`/contacts`) return 200 with exactly one H1, a `<title>`, a meta
description and a self-consistent canonical. JSON-LD is present on all
three (LocalBusiness plus BreadcrumbList sitewide; the dishwasher page adds
Service, FAQPage and HowTo). robots.txt, llms.txt and sitemap.xml (~34
URLs) are all present. robots.txt blocks no crawler of interest — Googlebot
is unrestricted and GPTBot, ClaudeBot, PerplexityBot, GoogleOther, CCBot,
Bytespider and anthropic-ai are each explicitly allowed; the only Disallow
rules cover PDFs.

**Accreditation check — clean.** The rule for this site is that the company
holds no Gas Safe, F-Gas or NICEIC accreditation. Occurrences of "Gas Safe"
and "F-Gas" in scraped copy and schema were checked individually and all
are references to the `/gas-safety` service page and its nav links (CP42
certification offered as a service), not claims that the company itself
holds an accreditation. llms.txt states the position explicitly: regulated
gas, F-Gas and electrical work "is carried out by qualified, appropriately
registered subcontract engineers." No copy requires removal.

## paw
No site defects found. Baseline established. The one P1 is a CONFIG
finding, recorded above rather than here.

### P3
- **robots.txt missing** — `/robots.txt` returns 404. Recommended action:
  add one, if only to carry a `Sitemap:` directive. Reported once; will be
  suppressed until it changes.
- **sitemap.xml missing** — `/sitemap.xml` returns 404. Recommended action:
  generate one at build time. Reported once; suppressed thereafter.
- **llms.txt missing** — `/llms.txt` returns 404. Recommended action: add
  one to describe the practice for AI crawlers. Reported once; suppressed
  thereafter.

Baseline state worth noting for future diffs (below threshold this run, no
action requested): the homepage has a title, meta description and one H1,
but carries no canonical tag and no JSON-LD. Once a baseline exists,
removal of either would trip P2 — currently there is nothing to remove.

## dower
No site defects found. Baseline established. The one P1 is a CONFIG
finding, recorded above rather than here.

Current state: homepage returns 200 with one H1, title, meta description,
canonical `https://dowerdigital.co.uk/`, and JSON-LD covering
ProfessionalService, WebSite and FAQPage. robots.txt, llms.txt and
sitemap.xml (6 URLs) are all present. robots.txt contains no Disallow rules
at all and explicitly allows thirteen crawlers including GPTBot, ClaudeBot,
PerplexityBot, GoogleOther, Google-Extended, Applebot-Extended and CCBot.

## Still outstanding
Nothing carried over — this is the first completed run, so there are no
prior open findings. The two P1 CONFIG entries raised today are now open in
`site-health/paw/open-findings.json` and
`site-health/dower/open-findings.json` and will be re-checked next week.

## Nothing changed
- **arch** — no findings this run.

## Drive handoff
`_site_health_archcateringandrefrigeration.co.uk.json` written to Drive
(file id `1VtAhvf_IaTE54l_ndIB3OLrEsjP7PQCY`) with `runs_this_month: 1`,
an empty findings list, and the uptime figure carrying its partial-coverage
caveat so the monthly report does not present ~7h of observation as a full
month. paw and dower have no `drive_file` configured, so nothing was
written for them.
