# Site health — week of 2026-08-15

> Note: an earlier attempt at this run today aborted before gathering any
> data because Firecrawl was not enabled for the session (see git history).
> That report has been superseded by this one, which ran with Firecrawl
> available. This remains a **first run** for all three sites — no prior
> baseline existed, so findings below describe current state, not a diff.

## At a glance
- **arch** — baseline established, 0 findings, uptime monitor UP (0
  incidents, but monitor is brand new — only ~7h of history)
- **paw** — baseline established, 1 P1 (contact page is dead), uptime
  UNAVAILABLE (no monitor)
- **dower** — baseline established, 1 P1 (contact page is dead), uptime
  UNAVAILABLE (no monitor)

## Data sources
- **Google Drive — UNAVAILABLE.** Connected at the org level but
  `enabledInChat: false` for this routine's session (the same class of
  issue that blocked Firecrawl on today's earlier aborted attempt). This
  blocks the arch Drive handoff — see Config below.
- **UptimeRobot** — connected and enabled. Only one monitor exists, for
  arch (`archcateringandrefrigeration.co.uk`). No monitor exists for paw
  (`positive-animal-wellbeing.netlify.app`) or dower (`dowerdigital.co.uk`)
  — flagged once here as a plumbing gap; availability was **not** inferred
  from Firecrawl succeeding for either site.
- **UptimeRobot (arch)** — the monitor was created today, 2026-08-15, so
  its 100%/0-incidents reading reflects roughly 7 hours of history, not a
  full week. Treat next week's figure as the first meaningful one.
- **Sentry** — connected and enabled, but all three sites are configured
  `sentry: NONE` (no project bound to any of them). Per the routine's
  rules, this is not reported as "0 errors" — error tracking simply
  wasn't evaluated this run for any site.
- **Search Console / Windsor** — intentionally out of scope for this
  weekly routine; covered by the monthly report's 28-day window instead.

## Config
- **P1 CONFIG** — The Google Drive connector is connected at the org
  level but not enabled for this routine's chat session, so
  `_site_health_archcateringandrefrigeration.co.uk.json` could not be
  written this run. The monthly report routine reads site health from
  that Drive file and cannot read this repository directly — without it,
  the monthly report will silently omit arch's site-health section.
  **Action needed:** enable the Google Drive connector for this routine's
  chat session (a per-session toggle, separate from the org-level
  authorization, which is already done).

## arch
### P1
None.

### P2
None.

### P3
None — robots.txt, llms.txt and sitemap.xml are all present and agree
with the configured domain.

### Still outstanding
None — first run.

**Baseline established — current state:**
- All 3 audited pages (`/`, `/commercial-dishwasher`, `/contacts`) scrape
  cleanly: each has a `<title>`, meta description, exactly one H1, and
  LocalBusiness/Service/FAQPage/BreadcrumbList JSON-LD. No soft-404s.
- **Accreditation check (Gas Safe / F-Gas / NICEIC):** no company-level
  claim of holding any of these found on the 3 audited pages. Where
  "F-Gas" or "Gas Safe" appear, they describe services offered, not
  credentials the company itself holds. `llms.txt` explicitly attributes
  Gas Safe registration and F-Gas qualification to **subcontracted
  engineers**, consistent with the accreditation_rule. One page outside
  this routine's scope — `/about-us#memberships-standards` ("Our
  Certifications & Standards") — was not checked; worth a look if a
  fuller audit is ever wanted, since its content wasn't verified here.
- `robots.txt` explicitly welcomes Googlebot (via the wildcard rule) and
  GPTBot, ClaudeBot, PerplexityBot, GoogleOther and CCBot (each with its
  own `Allow: /` block) — no Disallow rules block any of them.
- `llms.txt` and `sitemap.xml` both exist and are internally consistent
  with `archcateringandrefrigeration.co.uk`.
- **Config check:** canonical tags, robots.txt `Sitemap:` directive,
  sitemap.xml and llms.txt all agree on `archcateringandrefrigeration.co.uk`
  — no mismatch.
- UptimeRobot monitor exists and reports UP, 0 incidents (see Data
  sources note on limited history).

## paw
### P1
- **`/contact` returns a hard 404** (Netlify's stock "Page not found"
  page, HTTP 404 — not a soft-404, the route genuinely doesn't exist on
  the deployed site). Before → after: n/a (first run) — current state is
  broken. **Recommended action:** restore the `/contact` route, or if it
  was intentionally removed/renamed, update site navigation and any links
  pointing to it.

### P2
None.

### P3
- `robots.txt` missing (HTTP 404 at `/robots.txt`) — no crawl directives
  in place for any bot. **Recommended action:** add a minimal robots.txt.
- `llms.txt` missing (HTTP 404). **Recommended action:** optional; add
  one if AI-search visibility matters for this site.
- `sitemap.xml` missing (HTTP 404). **Recommended action:** add one for
  discoverability.
  (These three will be suppressed in future reports unless they change.)

### Still outstanding
None — first run.

**Baseline established — current state:**
- Homepage (`/`) scrapes cleanly — title, meta description, single H1 —
  but has no JSON-LD structured data and no `<link rel="canonical">` tag.
- No accreditation rule to police for this site.
- Config check could not be evaluated: none of the comparison artifacts
  (canonical tag, robots.txt, sitemap.xml, llms.txt) exist on the site,
  so there's nothing to compare the configured domain against. This is a
  data-availability gap, not a detected mismatch.

## dower
### P1
- **`/contact` returns a hard 404** (Netlify's stock "Page not found"
  page, HTTP 404) — the route doesn't exist and isn't listed in
  sitemap.xml either. This is Dower Digital's own site. **Recommended
  action:** restore the `/contact` route, or update navigation/sitemap if
  it was intentionally removed.

### P2
None.

### P3
None — robots.txt, llms.txt and sitemap.xml are all present and agree
with the configured domain.

### Still outstanding
None — first run.

**Baseline established — current state:**
- Homepage (`/`) scrapes cleanly with title, meta description, single
  H1, a canonical tag, and rich JSON-LD (ProfessionalService, FAQPage,
  Offer/Service catalog, WebSite).
- `robots.txt` explicitly welcomes Googlebot and all named AI crawlers
  (GPTBot, ClaudeBot, PerplexityBot, GoogleOther, CCBot) with no
  Disallow rules.
- `llms.txt` and `sitemap.xml` both exist and are internally consistent
  with `dowerdigital.co.uk`.
- **Config check:** canonical tag, robots.txt `Sitemap:` directive,
  sitemap.xml and llms.txt all agree on `dowerdigital.co.uk` — no
  mismatch.
- No accreditation rule to police for this site.
- No UptimeRobot monitor exists for this site (see Data sources).

## Nothing changed
N/A — this is the first completed run for all three sites; no prior
baseline existed to diff against.
