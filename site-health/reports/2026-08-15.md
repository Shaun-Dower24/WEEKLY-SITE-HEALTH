# Site health — week of 2026-08-15

## At a glance
- arch — OK, baseline established. Uptime 100% (partial coverage, ~16h window).
- paw — OK, baseline established. No uptime monitor.
- dower — OK, baseline established. No uptime monitor.

## Data sources
- **Firecrawl** — available and used for all page/robots.txt/llms.txt/sitemap.xml
  checks on all three sites this run.
- **UptimeRobot** — one monitor exists, for `archcateringandrefrigeration.co.uk`
  (id 803742553, status UP). It was created 2026-08-15T01:06:21Z — **under 7
  days old** — so this run's 100% figure covers only ~16h, not a full week;
  treat it as partial until the monitor has a full week behind it. No monitor
  exists for `positive-animal-wellbeing.netlify.app` or `dowerdigital.co.uk` —
  UPTIME_UNAVAILABLE for both, noted once as a plumbing gap (create monitors
  if weekly uptime tracking is wanted for these two).
- **Sentry** — organization `dower-digital` now has a real per-site project,
  `arch-catering`, bound to the arch site. Queried is:unresolved over the last
  30 days: 0 issues. `paw` and `dower` are configured `sentry: NONE` and were
  skipped entirely — not reported as "0 errors."
- **Google Drive** — available. Wrote the arch drive handoff file.

## Config
No CONFIG findings. All three sites' configured URLs agree with the domain
declared in canonical tags, robots.txt `Sitemap:` directives, and
sitemap.xml URLs.

## arch
First run for this site — no prior baseline existed, so this establishes one
rather than a diff.

- Pages checked (configured list): `/`, `/commercial-dishwasher`,
  `/contacts` — all returned 200, each with exactly one `<h1>`, a `<title>`,
  a meta description, a matching canonical, and `LocalBusiness` JSON-LD.
- robots.txt: present, allows Googlebot/GPTBot/ClaudeBot/PerplexityBot/
  GoogleOther/CCBot; `Sitemap:` points at the configured domain.
- llms.txt: present. sitemap.xml: present, 36 URLs.
- Accreditation check (company holds no Gas Safe/F-Gas/NICEIC
  accreditations itself): no violation found on the 3 configured pages or
  their JSON-LD. The dishwasher page's "Commercial Gas Safety" mention and
  the llms.txt description of subcontracted, appropriately registered
  engineers both describe services/subcontractors, not a company
  accreditation claim — not flagged, per the rule.
- Sentry (`arch-catering` project): 0 unresolved issues, last 30 days.
- Uptime: monitor UP since creation, 0 incidents. Coverage is **partial**
  (~16h) — the monitor is new; do not read this as a clean full week yet.

### Still outstanding
None.

## paw
First run for this site — baseline established.

- Page checked: `/` — 200, one `<h1>`, title and meta description present.
  No canonical tag.
- robots.txt, llms.txt, and sitemap.xml all return Netlify's default 404 —
  none exist on this site. Recorded once; will stay quiet on future runs
  unless this changes (per the P3 report-once rule).
- No accreditation rule to police on this site.
- No Sentry project, no UptimeRobot monitor — both configured `NONE`.

### Still outstanding
None.

## dower
First run for this site — baseline established. Pages list was `AUTO`, so it
was derived from sitemap.xml this run: homepage plus the three highest-
priority service pages (`/web-design/`, `/website-care/`, `/seo/`, all
priority 0.9), capped at 4. Excluded `/web-design-stevenage/` (priority 0.8,
a location variant) and `/faq/` (priority 0.7, not commercial). This
resolved list is now recorded in baseline.json and will stay stable between
runs.

- All 4 resolved pages returned 200, each with one `<h1>`, title, meta
  description, matching canonical, and `Service`/`BreadcrumbList` (service
  pages) or richer business/FAQ JSON-LD (homepage).
- robots.txt: present, allows the target search and AI crawlers;
  `Sitemap:` points at the configured domain.
- llms.txt: present. sitemap.xml: present, 6 URLs.
- No accreditation rule to police on this site.
- No Sentry project, no UptimeRobot monitor — both configured `NONE`.

### Still outstanding
None.

## Nothing changed
N/A — first run for all three sites, no prior baseline existed to diff
against.
