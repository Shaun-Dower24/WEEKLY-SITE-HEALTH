# Site health — week of 2026-08-15

This is the completed run for this date. An earlier same-day attempt aborted
because Firecrawl was not yet enabled for this session (see prior commit);
that has since been fixed and this run completed with all connectors live.

## At a glance
- arch — baseline established, 3 pages checked. No live defects found; the
  configured `/contact` URL is wrong (real page is `/contacts`, healthy).
- paw — baseline established, 2 pages checked. Missing robots.txt/sitemap/
  llms.txt; site lives only on a Netlify default subdomain.
- dower — baseline established, 2 pages checked. Routine is auditing a
  Netlify alias; the site's own signals (robots.txt, sitemap, llms.txt)
  point to a separate live production domain, dowerdigital.co.uk.

## Data sources
- **Firecrawl** — available and used for all scraping, robots/llms/sitemap
  checks, and reachability signals this run.
- **Windsor (GSC)** — connected. Only `arch` has a bound Search Console
  property (`https://archcateringandrefrigeration.co.uk/`); `paw` and
  `dower` are configured `gsc: NONE`. Arch's clicks/impressions/position
  came back all-zero for both the last 7 days (2026-08-08–08-14) and the
  prior 7 days (2026-08-01–08-07). Per the routine's rules this is reported
  as **DATA-UNAVAILABLE**, not a traffic collapse — a zero row can't be
  distinguished from an unverified property or a fresh connector. This is
  the same zero-data pattern the prior blocked run flagged; worth checking
  directly in Search Console for a property mismatch (e.g. a URL-prefix
  property for a different scheme/subdomain than what's actually serving)
  since Windsor shows the property is registered correctly as
  `https://archcateringandrefrigeration.co.uk/`.
- **Sentry** — connected, but the organization still has exactly one
  project (`javascript-react`) not bound to any of the three sites. Per
  the routine's rules, the error layer is skipped entirely for all sites
  again this run rather than report a misleading "0 errors."
- **UptimeRobot** — connected. One monitor exists, for
  `archcateringandrefrigeration.co.uk` (currently UP, no incidents). No
  monitors exist for `positive-animal-wellbeing.netlify.app` or
  `dower-digital.netlify.app` — recommend adding these per the routine's
  own prerequisite #5.
- **Routine config — arch `/contact`.** The configured page returns a soft
  404 (HTTP 200, but title "404" / H1 "Page Not Found" / no real content).
  This is not a site defect: the actual contact page is at `/contacts`
  (confirmed via sitemap.xml and internal links throughout the site) and
  is fully healthy — contact form, staff details, address, phone all
  present. Recommend updating this routine's `pages` list for arch from
  `/contact` to `/contacts`.
- **Routine config — dower domain.** This routine audits
  `dower-digital.netlify.app`, but that page's own robots.txt Sitemap
  directive, its sitemap.xml URLs, and its llms.txt all point to
  `dowerdigital.co.uk` — which resolves 200 with a self-referential
  canonical tag, i.e. a separate, live, canonical production domain. The
  netlify.app homepage's own `<link rel="canonical">` points to itself,
  which is inconsistent with its own `og:url` and llms.txt (both point to
  dowerdigital.co.uk). Recommend updating this routine's site config for
  `dower` to `https://dowerdigital.co.uk` so future runs check the
  canonical domain rather than the deploy alias.

## arch
### P3
- Configured `/contact` page is wrong — see "Routine config — arch
  `/contact`" above. Before → after: N/A (config fix, not a site change).
  Recommended action: update routine config to `/contacts`.

No other findings. Homepage and `/commercial-dishwasher` both returned
healthy titles, meta descriptions, canonicals, and JSON-LD (`Organization`,
`WebSite`, `WebPage` on the homepage; `WebPage` on the dishwasher page).
robots.txt allows all major crawlers and the full AI-crawler list checked
(GPTBot, ClaudeBot, PerplexityBot, GoogleOther, CCBot all `Allow`),
disallowing only a PDF folder. llms.txt and sitemap.xml are both present.

**Accreditation check (per client note: Arch holds no Gas Safe / F-Gas /
NICEIC accreditation — engineers are subcontracted):** checked homepage and
`/commercial-dishwasher`. Both mention "F-Gas" only as a service category
("F-Gas servicing and supply"), and llms.txt explicitly attributes regulated
gas/F-Gas/electrical work to "qualified, appropriately registered
subcontract engineers" — no direct claim that Arch itself holds these
accreditations. No P1. Note: `/gas-safety` and `/commercial-refrigeration`
weren't in this run's page list but reference certificates by name (CP42,
Gas Safe inspections) — worth a manual look given they sit closest to this
risk.

## paw
### P3
- No robots.txt, sitemap.xml, or llms.txt (all return Netlify's default
  404 page, confirmed on repeat checks). Before → after: N/A, first run.
  Recommended action: add a robots.txt at minimum; sitemap.xml and llms.txt
  if the site is meant to be indexed and AI-crawled.
- Site is live only on `positive-animal-wellbeing.netlify.app` — no custom
  domain detected in canonical, og:image, or favicon URLs, which all
  self-reference the Netlify subdomain. Netlify default subdomains carry
  no domain authority and some configurations serve `noindex` by default.
  Recommended action: confirm with the client whether this is the intended
  permanent public URL or a custom domain is planned.
- Configured `/contact` page is a real 404. No llms.txt exists to confirm,
  but the homepage's H1 pattern (a single narrative page: "Hello, I'm
  Christine", "Loved by pets and their people") suggests this is a
  single-page site with an in-page contact section rather than a separate
  route. Recommended action: confirm the real contact anchor/URL with the
  client and update routine config.

No other findings. Homepage returned a healthy title and meta description;
canonical is present (points to `/#home`). No JSON-LD found on the
homepage — captured as baseline state, not a finding, since there's no
prior baseline to diff against.

## dower
### P3
- Configured `/contact` page is a real 404 — confirmed via llms.txt this
  is `https://dowerdigital.co.uk/#contact`, an anchor on the homepage, not
  a separate route. Recommended action: update routine config or drop this
  page from the list.

See "Routine config — dower domain" above for the domain-mismatch finding
(not scored as a P-level site finding since it's a routine config issue,
not a site defect — the site itself is healthy on its real domain).

No other findings. Homepage returned a healthy title, meta description,
and `Organization` JSON-LD. robots.txt is thorough and explicitly welcomes
every AI crawler checked plus several more (GPTBot, OAI-SearchBot,
ChatGPT-User, ClaudeBot, anthropic-ai, Claude-Web, PerplexityBot,
Perplexity-User, Google-Extended, GoogleOther, Applebot-Extended, CCBot,
Bytespider). sitemap.xml (6 URLs) and llms.txt are both present and well
formed, on the canonical domain.

## Nothing changed
N/A — first run for all three sites, no prior baseline existed. Baselines
have been written to `site-health/<name>/baseline.json` and archived to
`site-health/<name>/history/2026-08-15.json`.
