# Site health — week of 2026-08-24

## At a glance
- **arch** — OK, no findings — uptime 100% (partial: 9 days since monitor created 2026-08-15)
- **paw** — 1 new finding (P3, informational), 2 still outstanding (P1, P2) — uptime: none (no monitor)
- **dower** — 1 new finding (P2) — uptime: none (no monitor)

## Data sources
- **Firecrawl** — available and used for all page, robots.txt, llms.txt and sitemap.xml checks this run.
- **UptimeRobot** — only `arch` has a monitor (created 2026-08-15, 9 days old — under a full month, so its uptime figure is reported as partial coverage rather than a clean month). `paw` and `dower` have no monitor configured: UPTIME_UNAVAILABLE, noted here as a plumbing gap, not inferred from Firecrawl reachability.
- **Sentry** — `arch-catering` project queried (0 issues first-seen in the last 7 days). `paw` is configured `sentry: NONE` in this routine's SITES list and was correctly skipped per that config — see **Config** below, this is itself a stale-config finding, not a clean bill of health. `dower` has no project and was skipped, not reported as "0 errors."
- **Google Search Console** — out of scope for this routine per its own rules; not queried.
- **Google Drive** — available. `arch`'s handoff file was refreshed with this run's uptime and findings.
- **PR pipeline** — this run's base branch (`claude/eager-planck-n9icg4`) had no `baseline.json`/`open-findings.json` for any site, because none of the six previous weekly PRs (#1–#6, 2026-08-15 through 2026-08-17) have been merged — they're all still open. So git-tracked state hasn't persisted between runs even though the routine has completed successfully at least twice before (Drive's `runs_this_month` counter for arch was already at 2). This run recovered last week's actual snapshot from the unmerged `site-health/2026-08-17` branch on GitHub to diff against properly, rather than treating this as a false "first run." **Recommend merging the open site-health PRs** (or squash-merging the backlog) so baselines persist in git and future runs don't need to reconstruct state from GitHub history.

## Config
- **paw — P1, still open (7 days).** Configured url `positive-animal-wellbeing.netlify.app` disagrees with the domain the site declares as canonical everywhere checked: the homepage `<link rel="canonical">`, robots.txt `Sitemap:` directive, sitemap.xml URLs, and llms.txt all point to `https://pawellbeing.com`. Corrected value to paste in: `url: "https://pawellbeing.com"`. Continuing to audit the configured url per routine rules, so this diff stays comparable.
- **paw — P2, still open (7 days).** Stored SITES config says `sentry: NONE` for paw, but a `paw` project exists in the `dower-digital` Sentry org and the live site is instrumented against it. Corrected value to paste in: `sentry: paw`. Until this is updated, scheduled runs keep silently skipping error monitoring for this site.

## paw
### P3
- New page discovered — sitemap.xml grew from 12 to 13 URLs — added `/pulsed-magnetic-field-therapy` (lastmod 2026-08-20) — informational only, no action needed.

### Still outstanding
- **[P1, 7 days]** Configured host mismatch — routine audits `positive-animal-wellbeing.netlify.app`, site declares `pawellbeing.com` as canonical — fix: update SITES config url.
- **[P2, 7 days]** Sentry config stale — routine config says `sentry: NONE`, a real `paw` project exists — fix: update SITES config to `sentry: paw`.

## dower
### P2
- JSON-LD schema block removed from the homepage — before: `@graph` held 6 top-level nodes (`ProfessionalService`, `Person`, `OfferCatalog`, `Service`, `WebSite`, `FAQPage`) as of 2026-08-17 — after: only 3 remain (`ProfessionalService`, `WebSite`, `FAQPage`); `Person`, `OfferCatalog` (with its `Offer`/pricing children) and the standalone `Service` nodes are gone. The three service pages (`/web-design/`, `/website-care/`, `/seo/`) still carry their own `Service` schema independently, so this is a homepage-only loss. Recommend restoring the missing nodes to the homepage `@graph`, or confirming the removal was intentional (e.g. a recent template change).

## Nothing changed
- arch — no findings this run; uptime 100% over the monitor's 9-day history to date (partial coverage, not yet a full month).
