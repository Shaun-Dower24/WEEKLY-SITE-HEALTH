# Site health — week of 2026-09-07

## At a glance
- arch — OK, uptime 100% (full coverage, 0 incidents)
- paw — 1 finding (P1 CONFIG, internal-only, unchanged), uptime UNAVAILABLE (no monitor)
- dower — OK, uptime UNAVAILABLE (no monitor)

## Data sources
- **Sentry — now available.** Unlike the 2026-08-31 run (blocked on OAuth in
  the unattended session), this run queried the `arch-catering` project
  successfully: 0 issues first-seen or regressed in the 7 days since the last
  run, 0 incidents. `paw` and `dower` still have no Sentry project configured
  (`sentry: NONE`) and were skipped, as designed — not reported as "0 errors".
- **UptimeRobot — still only one monitor.** Only `archcateringandrefrigeration.co.uk`
  has a monitor; `paw` and `dower` remain UPTIME_UNAVAILABLE, unchanged since
  the last run. Availability is not inferred from Firecrawl succeeding.
- **GSC/Windsor** — not queried, out of scope by design.
- Firecrawl and Google Drive were both available and used normally.
- **Process note:** every previous weekly run (2026-08-15 through 2026-08-31,
  PRs #1–#8) was opened against `claude/elegant-mayer-k2syn6` but never
  merged, so `site-health/*/baseline.json` never landed on that branch and
  each run was re-declaring "first run". This run pulled the most recent
  baseline/open-findings/history files forward from the unmerged
  `site-health/2026-08-31` branch so this week's diff is meaningful. **Action
  needed:** merge this PR (or at least one recent site-health PR) so future
  runs stop losing state.

## Config
- **paw: configured URL still does not match the site's declared domain**
  (unchanged from 2026-08-31). The routine is pointed at
  `https://positive-animal-wellbeing.netlify.app`, but the page's canonical
  tag, robots.txt `Sitemap:` directive, sitemap.xml and llms.txt all still
  declare **`https://pawellbeing.com`**. Continuing to audit the configured
  Netlify URL so the diff stays comparable. **Corrected value to paste into
  config:** `url: "https://pawellbeing.com"`. Routine-configuration issue,
  not a site defect — `client_visible: false`.

## arch
Nothing changed since 2026-08-31. All 3 configured pages (`/`,
`/commercial-dishwasher`, `/contacts`) still scrape cleanly with stable
titles, meta descriptions, single H1s and JSON-LD. robots.txt, sitemap.xml
and llms.txt still agree on `archcateringandrefrigeration.co.uk`.
Accreditation check: no violation (Gas Safe/F-Gas/CP42 mentions remain
service descriptions or subcontracted-engineer credentials, not company
claims). Sentry: 0 new/regressed issues. Uptime: 100%, 0 incidents, full
30-day coverage.

### Still outstanding
None.

## paw
Nothing changed since 2026-08-31. Homepage still scrapes cleanly; title,
meta description and JSON-LD set unchanged. Uptime UNAVAILABLE — no monitor
exists for this site.

### Still outstanding
- Configured URL is the site's old Netlify address; real domain is
  `pawellbeing.com` (P1, CONFIG, internal-only) — found 2026-08-31, 7 days
  old. See Config section above for the corrected value.

## dower
Nothing changed since 2026-08-31. All 4 AUTO-resolved pages (`/`,
`/web-design/`, `/website-care/`, `/seo/`) still scrape cleanly with stable
titles, meta descriptions and JSON-LD. Uptime UNAVAILABLE — no monitor
exists for this site.

### Still outstanding
None.

## Nothing changed
- arch — no findings this week.
- dower — no findings this week.
