# Site health — week of 2026-08-31

## At a glance
- arch — OK, baseline established, uptime 100% (16d coverage, no incidents)
- paw — 1 finding (P1 CONFIG, internal-only), baseline established, uptime UNAVAILABLE (no monitor)
- dower — OK, baseline established, uptime UNAVAILABLE (no monitor)

## Data sources
- **Sentry — UNAVAILABLE for all sites.** The Sentry MCP connector requires an
  OAuth authorization this unattended session cannot complete. For `arch`
  (project `arch-catering`), this means no error data was gathered this run —
  a plumbing gap, not a clean bill of health. `paw` and `dower` have no Sentry
  project configured (`sentry: NONE`), so they were skipped entirely as
  designed. **Action needed:** authorize the Sentry connector for this
  routine's session (via `claude mcp` / `/mcp` interactively) so the next run
  can query it.
- **UptimeRobot — no monitor exists for `paw` or `dower`.** Only one monitor
  exists on the account, for `archcateringandrefrigeration.co.uk`. Uptime is
  UNAVAILABLE for the other two sites; do not infer availability from
  Firecrawl succeeding. **Action needed:** create UptimeRobot monitors for
  `pawellbeing.com` and `dowerdigital.co.uk` if uptime tracking is wanted for
  them.
- **GSC/Windsor** — not queried this run by design (out of scope per routine
  rules; covered by the monthly report's 28-day window instead).
- Firecrawl and Google Drive were both available and used normally this run.

## Config
- **paw: configured URL does not match the site's declared domain.** The
  routine is pointed at `https://positive-animal-wellbeing.netlify.app`, but
  the page's own canonical tag, robots.txt `Sitemap:` directive, sitemap.xml
  URLs and llms.txt all declare **`https://pawellbeing.com`** as the real
  domain — the site has moved off the Netlify subdomain onto its own domain.
  Firecrawl's resolved metadata URL for every fetch also came back as
  `pawellbeing.com`. Per the routine's rule, this run continued auditing the
  configured Netlify URL (which still resolves and serves the same content)
  so the diff stays comparable, and logs the mismatch here rather than
  silently following the canonical.
  **Corrected value to paste into config:** `url: "https://pawellbeing.com"`
  This is a routine-configuration issue, not a defect on the site, and is
  recorded as `client_visible: false` in open-findings.

## arch
FIRST RUN — baseline established. Current state:
- All 3 configured pages (`/`, `/commercial-dishwasher`, `/contacts`) scrape
  cleanly (HTTP 200), each with a title, meta description, single H1, and
  LocalBusiness + Service/BreadcrumbList JSON-LD.
- robots.txt allows Googlebot, GPTBot, ClaudeBot, PerplexityBot, GoogleOther,
  CCBot and others; only the legal-policy PDFs are disallowed. Sitemap
  directive, sitemap.xml and llms.txt all agree on
  `archcateringandrefrigeration.co.uk` — matches the configured URL.
- **Accreditation check: no violation.** References to "Gas Safe", "F-Gas"
  and "CP42 certification" on the checked pages are service descriptions
  (e.g. "Gas safety inspections and CP42 certification for commercial
  kitchens") or describe the credentials of subcontracted engineers per
  llms.txt ("regulated work... carried out by qualified, appropriately
  registered subcontract engineers"). No page claims the company itself
  holds Gas Safe, F-Gas or NICEIC accreditation.
- Sentry: UNAVAILABLE this run (see Data sources).
- Uptime: 100% since monitor creation (2026-08-15, 16 days, full coverage),
  0 incidents.

### Still outstanding
None.

## paw
FIRST RUN — baseline established. Current state:
- Homepage scrapes cleanly (HTTP 200): title, meta description, single H1,
  and a rich JSON-LD set (LocalBusiness, FAQPage, Service, Person, etc.).
- robots.txt explicitly welcomes Googlebot, Bingbot, and the full set of AI
  crawlers (GPTBot, ClaudeBot, PerplexityBot, Google-Extended, Applebot,
  CCBot, etc.).
- Accreditation: none to police for this site.
- Sentry: not configured (NONE) — skipped as designed.
- Uptime: UNAVAILABLE — no monitor exists (see Data sources).

### Still outstanding
- **Configured URL is the site's old Netlify address; real domain is
  pawellbeing.com** (P1, CONFIG, internal-only) — found 2026-08-31, 0 days
  old. See Config section above for the corrected value.

## dower
FIRST RUN — baseline established. **Pages list resolved via AUTO for the
first time**: sitemap.xml gave homepage + 6 other URLs; the 3 highest-priority
(0.9) commercial pages were selected alongside the homepage, capped at 4:
`/`, `/web-design/`, `/website-care/`, `/seo/`. (`/web-design-stevenage/` at
priority 0.8, and the non-commercial `/faq/` and `/privacy/`, were excluded.)
This resolved list is now recorded in baseline.json and will stay stable
between runs.

- All 4 resolved pages scrape cleanly (HTTP 200), each with a title, meta
  description, single H1, and Service + BreadcrumbList JSON-LD on the service
  pages (ProfessionalService + FAQPage + WebSite on the homepage).
- robots.txt welcomes all major search and AI crawlers. Sitemap directive,
  sitemap.xml and llms.txt all agree on `dowerdigital.co.uk` — matches the
  configured URL.
- Accreditation: none to police for this site.
- Sentry: not configured (NONE) — skipped as designed.
- Uptime: UNAVAILABLE — no monitor exists (see Data sources).

### Still outstanding
None.

## Nothing changed
N/A — first run for all three sites, no prior baseline existed to diff
against.
